# onnxruntime-node bundles its CPU binary inside the package. Skipping its
# postinstall avoids downloading the unused CUDA EP binaries from NuGet during
# `npm ci`; the CPU build is all scripts/precompute-embeddings.mjs needs.
onnxruntime-node-install=skip
